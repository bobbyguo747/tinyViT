# Transformer 项目答辩问答

## 30 秒项目介绍

我使用 PyTorch 在公共数据集 Fashion-MNIST 上从零实现了一个 TinyViT，没有调用完整 ViT 或预训练权重。模型把 28×28 灰度图切成 49 个 4×4 patch，使用 64 维 embedding、4 个注意力头和 2 层 Encoder，通过 CLS token 完成 10 类分类。我还进行了 256 样本过拟合诊断、CNN 基线和位置编码消融。实际测试 Accuracy 分别为 CNN 91.92%、TinyViT 87.78%、无位置编码 TinyViT 83.09%。结果说明 CNN 更适合当前小型图像条件，同时位置编码对 TinyViT 有明显帮助。

## 1. 为什么选择 Fashion-MNIST？

它是公开、类别均衡的图像分类数据集，比手写数字更有区分难度，同时规模和分辨率适合在个人 GPU 上快速完成多组真实对照实验。

## 2. 什么是 patch？

patch 是图像中的一个小块。项目将 28×28 图像切成 4×4 的不重叠小块，每边 7 个，共 49 个，并将每个小块投影成 64 维 token。

## 3. 为什么使用 CLS token？

CLS 是一个可学习的汇总 token。它与所有 patch 一起经过自注意力，最后取其 64 维向量送入分类层，输出 10 类 logits。

## 4. 为什么需要位置编码？

自注意力主要比较 token 内容，本身无法直接知道 patch 位于左上角、中心还是右下角。位置编码为 token 提供空间顺序。移除位置编码后测试 Accuracy 从 87.78% 降至 83.09%。

## 5. Q、K、V 分别是什么？

Q 表示当前 token 想查找的信息，K 表示 token 可被匹配的特征，V 是被加权汇总的内容。通过 `Softmax(QKᵀ/√d)V` 得到上下文表示。

## 6. 为什么除以根号 d？

维度增大时 QK 点积的数值可能变大，使 Softmax 过度尖锐、梯度不稳定。除以 `√d` 可以控制尺度。

## 7. 多头注意力有什么作用？

不同注意力头在不同特征子空间中学习关系。本项目 64 维分为 4 个头，每头 16 维，最后重新合并为 64 维。

## 8. Encoder 包含什么？

每层包含 LayerNorm、多头自注意力、残差连接、MLP 和第二个残差连接。项目使用两层 Pre-LayerNorm Encoder。

## 9. 为什么模型末尾没有 Softmax？

`CrossEntropyLoss` 直接接收原始 logits，内部完成适合数值稳定计算的 LogSoftmax。训练前手动 Softmax 会造成重复处理。

## 10. 交叉熵衡量什么？

它衡量预测类别分布与真实标签之间的差距。模型对正确类别给出的相对分数越高，loss 通常越低。

## 11. 梯度和反向传播是什么？

梯度表示参数变化对 loss 的影响方向和大小。`loss.backward()` 使用链式法则计算梯度，AdamW 的 `step()` 根据梯度更新参数。

## 12. 为什么划分训练、验证和测试集？

训练集更新参数，验证集选择最佳 epoch，测试集只报告最终泛化性能。若使用测试集调参，测试结果将不再独立可信。

## 13. 小数据过拟合诊断有什么用？

如果模型连 256 张训练图片都无法记忆，说明训练链路、模型容量或超参数可能有问题。实际达到 99.22%，证明基本学习能力正常，但它不是正式成绩或预训练。

## 14. 如何选择 best.pt？

每轮训练后在验证集计算 Accuracy 和 Macro-F1。Accuracy 更高时保存；Accuracy 相同时使用 Macro-F1 决胜。测试集不参与选择。

## 15. Accuracy 与 Macro-F1 有什么区别？

Accuracy 是总体正确比例。Macro-F1 先计算每类 F1 再等权平均，更能反映各类别表现是否均衡。Fashion-MNIST 类别均衡，因此两者比较接近。

## 16. 如何阅读混淆矩阵？

行是真实类别，列是预测类别；对角线表示正确分类，非对角线表示错分。TinyViT 主要混淆 T-shirt、Shirt、Pullover 和 Coat 等相似上衣。

## 17. 为什么 CNN 比 TinyViT 更好？

Fashion-MNIST 分辨率低，分类依赖局部边缘和轮廓。CNN 的局部连接和权重共享提供了适合该任务的归纳偏置。这个结果不能推广为 CNN 永远优于 Transformer。

## 18. 项目是否使用预训练模型？

没有。TinyViT 和 CNN 均从随机初始化开始训练，也没有使用 `timm` 或 torchvision 的完整 ViT。

## 19. 项目的主要局限是什么？

每个模型只运行一个随机种子，没有针对每种模型分别搜索最优参数，没有数据增强或学习率调度，Fashion-MNIST 也不能代表大型彩色图像任务。

## 20. 可以怎样继续改进？

可以增加多个随机种子并报告均值和标准差，加入轻量数据增强或学习率调度，也可以在保持参数量接近的条件下研究 patch size、层数和 embedding 维度。

