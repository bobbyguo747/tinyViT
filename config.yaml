seed: 42
device: auto

data:
  root: data
  val_size: 6000
  batch_size: 128
  num_workers: 0
  pin_memory: true
  mean: 0.2860
  std: 0.3530

model:
  image_size: 28
  in_channels: 1
  num_classes: 10
  patch_size: 4
  embed_dim: 64
  num_heads: 4
  num_layers: 2
  mlp_ratio: 4
  dropout: 0.1

training:
  epochs: 20
  learning_rate: 0.001
  weight_decay: 0.0001

